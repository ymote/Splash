[package]
name = "splash-core"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Bounded, host-neutral Splash VM runtime"

[lints]
workspace = true

[dependencies]
makepad-script = { path = "../../vendor/makepad/platform/script" }
