let profile = {
    name: "Ada",
    active: true,
    nested: {level: 1, flags: {ignored: true}}
}
let alias = profile
alias.nested.level
let selected = profile.nested
let selected_alias = selected
let flags = selected_alias.flags
flags.ignored
let compact_flags = profile.nested.flags
compact_flags.ignored
