use mod.std.object
let merged = object.merge({left: 1}, {right: 2})
object.has(merged, "left")
object.get(merged, "missing", 0)
object.pick(merged, ["left"])
object.omit(merged, ["right"])
object.from_entries([["left", 1]])
object.with(merged, "left", 3)
object.keys(merged)
