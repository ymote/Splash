use mod.std.array
let selected = array.slice([1, 2, 3], 1, 3)
array.has_index(selected, 0)
array.get(selected, 2, 0)
array.contains(selected, 2)
array.index_of(selected, 2)
array.flatten([selected, [4]])
array.compact([nil, 1, nil])
array.range(0, 3)
array.unique([1, 1, 2])
array.reverse(selected)
